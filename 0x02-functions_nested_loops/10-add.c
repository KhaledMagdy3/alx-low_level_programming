#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 *
 * @x: integer
 * @y: integer
 *
 * Return: integer
 */

int add(int x, int y)
{
	int u;

	u = x + y;

	return (u);
}
